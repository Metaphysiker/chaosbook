
<h1>Welcome to Chaosbooks!</h1>

Ever had a great idea and a convincing start, but you never could get the job done? Let others finish your work!

<h1>Show all chaosbooks</h1>

<% @books.each do |book| %>
    <div class="imagedropshadow">
  Title: <%= link_to book.title, book_path(book) %> <br><br>
      Description: <br>
      <div class="imagedropshadow">
  <%= book.description %><br>
      </div>
  maximum amount of chapters: <%= book.maxnumchapt %><br>
      <% book.tags.any? %>
      <% book.tags.each do |tag| %>
     <span class="label label-teal">
        <%= link_to tag.name, tag_path(tag.name) %>
     </span>
      <% end %><br>
    </div>
<% end %>

